---
name: /upload_session/start
route: /files/upload_session/start
namespace: files
version: 1
menu: files
description: Upload sessions allow you to upload a single file in one or more requests, for example where the size of the file is greater than 150 MB.  This call starts a new upload session with the given data. You can then use `upload_session/append:2` to add more data and `upload_session/finish` to save all the data to a file in Dropbox.\nA single request should not upload more than 150 MB. The maximum size of a file one can upload to an upload session is 350 GB.\nAn upload session can be used for a maximum of 48 hours. Attempting to use an `UploadSessionStartResult.session_id` with `upload_session/append:2` or `upload_session/finish` more than 48 hours after its creation will return a `UploadSessionLookupError.not_found`.\nCalls to this endpoint will count as data transport calls for any Dropbox Business teams with a limit on the number of data transport calls allowed per month. For more information, see the :link:`Data transport limit page https://www.dropbox.com/developers/reference/data-transport-limit`.
isDeprecated: false
urlStructure: https://content.dropbox.com/2/files/upload_session/start
endpointFormat: upload
isPreview: false
scope: files.content.write
authentication:
  - user
  - team_admin
shellExample:
  label: command line example
  content: curl -X POST https://content.dropboxapi.com/2/files/upload_session/start \\n    --header "Authorization: Bearer YOUR_ACCESS_TOKEN" \\n    --header "Dropbox-API-Arg: {\"close\": false}" \\n    --header "Content-Type: application/octet-stream" \\n    --data-binary @local_file.txt
paramType: 
  namespace: files
  datatype: UploadSessionStartArg
paramExamples: 
  - label: default
    content: {"close": false}
returnType: 
  namespace: files
  datatype: UploadSessionStartResult
returnExamples: 
  - label: default
    content: {"session_id": "1234faaf0678bcde"}
errorType: 
  namespace: Void
  datatype: Void
---

import Endpoint from '../../components/Endpoint'
import stoneTypes from '../../type-lookup/files-upload_session-start.js'

<Endpoint endpointProps={{typeInfo: stoneTypes, ...props.pageContext.frontmatter}} />
