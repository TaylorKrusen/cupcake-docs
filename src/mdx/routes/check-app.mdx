---
name: /app
route: /check/app
namespace: check
version: 1
menu: check
description: This endpoint performs App Authentication, validating the supplied app key and secret, and returns the supplied string, to allow you to test your code and connection to the Dropbox API. It has no other effect. If you receive an HTTP 200 response with the supplied query, it indicates at least part of the Dropbox API infrastructure is working and that the app key and secret valid.
isDeprecated: false
urlStructure: https://api.dropbox.com/2/check/app
endpointFormat: rpc
isPreview: true
scope: None
authentication:
  - app
shellExample: 'curl -X POST https://api.dropboxapi.com/2/check/app \\n    --header "Authorization: Basic YOUR_APP_KEY:YOUR_APP_SECRET" \\n    --header "Content-Type: application/json" \\n    --data "{\"query\": \"foo\"}"'
paramType: 
  namespace: check
  datatype: EchoArg
paramExamples: 
  - label: default
    content: {"query": "foo"}
returnType: 
  namespace: check
  datatype: EchoResult
returnExamples: 
  - label: default
    content: {"result": "foo"}
errorType: 
  namespace: Void
  datatype: Void
---

import Endpoint from '../../components/Endpoint'
import stoneTypes from '../../type-lookup/check-app.js'

<Endpoint endpointProps={{typeInfo: stoneTypes, ...props.pageContext.frontmatter}} />
